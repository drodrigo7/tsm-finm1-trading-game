# Portfolio Analysis Game

## Environment
Windows virtual environment configuration:
```bash
cd app
python -m venv .venv
.venv\Scripts\activate.bat
pip3 install -r requirements.txt
```

## Metadata
Metadata at `./assets/metadata.xlsx`.
